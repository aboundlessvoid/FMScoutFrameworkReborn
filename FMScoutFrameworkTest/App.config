<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Application configuration kept for compatibility. .NET (Core/5+) uses runtimeconfig.json instead of supportedRuntime entries. -->
</configuration>
